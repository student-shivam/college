VITE_API_BASE_URL=http://localhost:5000/api
# Production example:
# VITE_API_BASE_URL=https://<your-backend>.onrender.com/api
# If you deploy on Netlify with `frontend/netlify.toml`, you can also leave this unset
# and let the app use `/api` (Netlify rewrites it to Render).
